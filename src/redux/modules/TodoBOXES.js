const initialState = [
    { id: Math.random(), jaemok: "몰루", naeyong: "뭘봐", isDone: false }
];

//리듀서
const TodoBOXES = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default TodoBOXES